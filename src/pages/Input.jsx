import React, { Fragment } from "react";
import FormInput from "../component/FormInput";

const Input = () => {
  return (
    <Fragment>
      <FormInput />
    </Fragment>
  );
};

export default Input;
